{
  "model": "Gemini 3.1 Pro (High)",
  "permissions": {
    "allow": [
      "command(dir)",
      "command(tree)",
      "command(type)",
      "command(findstr)",
      "command(where)",
      "command(git status)",
      "command(git log)",
      "command(git diff)",
      "command(git show)",
      "command(git ls-files)",
      "command(Get-ChildItem)",
      "command(Get-Content)",
      "command(Select-String)",
      "command(Measure-Object)",
      "command(ls)",
      "command(cat)",
      "command(grep)",
      "command(rg)",
      "command(head)",
      "command(tail)",
      "command(wc)",
      "command(find)",
      "command(pwd)",
      "command(git)",
      "command(echo hello)"
    ],
    "deny": [
      "command(rm)",
      "command(Remove-Item)",
      "command(del)",
      "command(git push)",
      "command(git reset)"
    ]
  },
  "trustedWorkspaces": [
    "D:\\Electrónica\\Electron Art\\Circuitron",
    "D:\\Portfolio"
  ]
}
